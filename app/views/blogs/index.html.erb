<p id="notice"><%= notice %></p>
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
  <h1 class="h2">Distributor List</h1>
  <div class="btn-toolbar mb-2 mb-md-0">
    <div class="btn-group mr-2">
      <button class="btn btn-sm btn-outline-secondary"><%= link_to '承認済み', blogs_path(status:2) %></button>
      <button class="btn btn-sm btn-outline-secondary"><%= link_to ' 保留中 ', blogs_path(status:0) %></button>
      <button class="btn btn-sm btn-outline-secondary"><%= link_to ' 否決済み', blogs_path(status:1) %></button>
      <button class="btn btn-sm btn-outline-secondary"><%= link_to ' 全て', blogs_path %></button>
      <button class="btn btn-sm btn-outline-secondary"><%= link_to ' Export xlsx' %></button>
    </div>
    <!-- <button class="btn btn-sm btn-outline-secondary dropdown-toggle">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewbox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-calendar">
        <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
        <line x1="16" y1="2" x2="16" y2="6"></line>
        <line x1="8" y1="2" x2="8" y2="6"></line>
        <line x1="3" y1="10" x2="21" y2="10"></line>
      </svg>
      This week
    </button> -->
  </div>
</div>


<br>

<% if current_user.try(:admin?) %>
  <h1>Blogs_adminとしてログイン中です</h1>
<% else %>
  <h1>アドミンではありません</h1>
<% end %>


<main>
  <div class="table-responsive">
    <table class="table table-striped table-sm">
      <thead>
        <tr>
          <th>ID</th>
          <th>title</th>
          <th>content</th>
          <th>status</th>
        </tr>
      </thead>
      <tbody>
        <% @blogs.each do |blog| %>
          <tr>
            <td><%= blog.id %></td>
            <td><%= blog.title %></td>
            <td><%= blog.content %></td>
            <td><%= blog.status %></td>
            <td><%= link_to 'Show', blog %></td>
            # Unapprovedか、on_holdの場合は場合分けででりーともしくは編集できるようにする
            <!-- <td><%= link_to 'Edit', edit_blog_path(blog) %></td> -->
            <!-- <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</main>

<%= link_to 'New Distributor', new_blog_path, class:"btn btn-outline-secondary" %>
